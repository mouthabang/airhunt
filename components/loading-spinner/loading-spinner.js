// components/loading-spinner/loading-spinner.js
Component({
  props: {
    size: "48px", // Default size
    color: "#ff4d4f", // Default color
    backgroundColor: "rgba(255, 77, 79, 0.2)" // Default background color
  },
  
  // Component initialization
  didMount() {
    // Any initialization code can go here if needed
  }
});